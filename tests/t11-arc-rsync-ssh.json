{
	"_comment": "C1/R2-arc: rsync targeting ssh dir. rsync -a ~/.ssh/ /tmp/t11-ssh-backup/. EXPECT: DENY on patched, ALLOW on live.",
	"tool_name": "Bash",
	"tool_input": { "command": "rsync -a ~/.ssh/ /tmp/t11-ssh-backup-decoy/" }
}
