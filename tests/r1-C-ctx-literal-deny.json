{
	"tool_name": "mcp__plugin_context-mode_context-mode__ctx_execute",
	"tool_input": {
		"language": "python",
		"code": "# INERT — control: literal path triggers sentinel\nimport pathlib\np = pathlib.Path('~/.ssh/id_rsa').expanduser()\nprint(p.read_text())"
	}
}
