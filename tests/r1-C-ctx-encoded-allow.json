{
	"tool_name": "mcp__plugin_context-mode_context-mode__ctx_execute",
	"tool_input": {
		"language": "python",
		"code": "# INERT proof-of-bypass — no literal credential path; runtime construction evades sentinel\nimport base64, pathlib, os\n# decode at runtime: base64.b64decode('fi5zc2gvaWRfcnNh') == b'~/.ssh/id_rsa'\npath_bytes = base64.b64decode('fi5zc2gvaWRfcnNh')\ntarget = pathlib.Path(path_bytes.decode()).expanduser()\n# INERT: just printing the path, not actually reading\nprint('would read:', target)\npass"
	}
}
