{
	"_comment": "R4-N: chflags -R schg targeting shallow home subdir. EXPECT: DENY on patched, ALLOW on live.",
	"tool_name": "Bash",
	"tool_input": { "command": "chflags -R schg ~/Projects-decoy" }
}
