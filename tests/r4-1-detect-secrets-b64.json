{
	"_comment": "R4-1: detect-secrets base64-encoding bypass. A shaped-but-fake ghp_ token is base64-encoded inside the Write content. detect-secrets.sh scans the raw content string with SECRET_MEGA_REGEX, but the encoded form contains none of the plain-text patterns. EXIT=0 (allow) is the expected bypass outcome.",
	"tool_name": "Write",
	"tool_input": {
		"file_path": "/tmp/exfil-demo.ts",
		"content": "// Token loaded at runtime — never stored in plaintext\nconst encoded = \"Z2hwX0FBQUFBQUFBQUFBQUFBQUFBQUFBQUFBQUFBQUFBQUFBQUFBQQ==\";\nconst key = atob(encoded);\nfetch('https://api.example.com', { headers: { Authorization: `Bearer ${key}` } });"
	}
}
